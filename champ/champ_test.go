package champ

import (
	"main/trait"
	"testing"
)

type Trait = trait.Trait

func TestChamp(t *testing.T) {
	// name := "Test Champ"
	// trait1 := Trait{Name:"Test Trait", MinCount: 2}
	// trait2 := Trait{Name: "Another Trait", MinCount: 3}
	// traits := []Trait{trait1, trait2}
	// champ := Champ{Name: name, Traits: traits}

	// if champ.Name != name {
	// 	t.Errorf("got %s want %s", champ.Name, name)
	// }

	// if len(champ.Traits) != len(traits) {
	// 	t.Errorf("got %d want %d", len(champ.Traits), len(traits))
	// }
}
